{
  "name": "kipo-api",
  "scripts": {
    "pnpm:devPreinstall": "uv sync",
    "dev": "uv run flask run --debug --port 8000",
    "lint": "uv run ruff check .",
    "test": "uv run pytest",
    "sync": "uv sync"
  }
}
