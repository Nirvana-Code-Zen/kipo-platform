'use strict'

const colors = require('./src/colors.json')
const typography = require('./src/typography.json')
const spacing = require('./src/spacing.json')

module.exports = { colors, typography, spacing }
